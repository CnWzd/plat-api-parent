% MyBatis-Flex APT 配置
% TableDef 采用默认生成位置：实体同包（com.zdd.plat.admin.entity[.log]）
% 自定义 processor.tableDef.package 在 Maven annotationProcessorPaths 场景下不稳定，故使用默认值
processor.enable=true
processor.mapper.generateEnable=false
